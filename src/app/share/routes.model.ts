import { Time } from "@angular/common";

export class Routes {
    routeId: number;
    start: string;
    end: string;
    startDate: string;
    startTime: string;
    price: number;
    route: string;
}